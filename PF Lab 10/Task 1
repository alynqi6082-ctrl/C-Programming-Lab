#include<stdio.h>
#include<string.h>
int main(){
	char word[100];
	int length=0;
	
	printf("Enter a code word: ");
	scanf("%s",&word);
	
	while(word[length]!='\0'){
		length++;
	}
	
	printf("The code %s has %d characters ",word,length);
	return 0;
}
