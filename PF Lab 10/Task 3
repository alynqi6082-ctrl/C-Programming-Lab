#include <stdio.h>
#include <string.h>

int main() {
    char c1[50], c2[50];
    int n;
    
    scanf("%s", c1);
    scanf("%s", c2);
    scanf("%d", &n);
    
    if (strncmp(c1, c2, n) == 0)
        printf("Same prefix");
    else
        printf("Different prefix");
    return 0;
}

