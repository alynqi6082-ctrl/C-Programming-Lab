#include <stdio.h>

int main() {
    int *ptr = NULL; 

    if (ptr == NULL) {
        printf("Pointer does not reference any valid memory.\n");
    } else {
        printf("Pointer is valid and points to a memory location.\n");
    }

    return 0;
}

