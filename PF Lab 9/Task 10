#include <stdio.h>

float average(int a, int b, int c) {
    return (a + b + c) / 3.0;
}

int main() {
    int x = 12, y = 18, z = 24;
    float avg = average(x, y, z);

    printf("Average of %d, %d, %d = %.2f\n", x, y, z, avg);

    return 0;
}

